# Optional: explicit API base URL used by axios in src/services/api.js
# For local dev with Vite proxy, leave unset or use /api
VITE_API_BASE_URL=/api

# Optional: Vite dev proxy target (used in vite.config.js)
VITE_API_PROXY_TARGET=http://localhost:8080
